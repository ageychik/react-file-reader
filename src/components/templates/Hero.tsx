import styled from "styled-components";

const HeroWrapper = styled.section`
  
`

const Hero = (props:any) => {
    return (
        <HeroWrapper>

        </HeroWrapper>
    )
}

export default Hero;